---
title: collections
description: The collections module in gopy.
---

# `collections`

The `collections` module. Behaviour matches CPython 3.14. Source lives at
`module/collections/` in the gopy repo (or under `stdlib/collections.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
