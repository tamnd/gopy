---
title: dataclasses
description: The dataclasses module in gopy.
---

# `dataclasses`

The `dataclasses` module. Behaviour matches CPython 3.14. Source lives at
`module/dataclasses/` in the gopy repo (or under `stdlib/dataclasses.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
