---
title: os
description: The os module in gopy.
---

# `os`

The `os` module. Behaviour matches CPython 3.14. Source lives at
`module/os/` in the gopy repo (or under `stdlib/os.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
