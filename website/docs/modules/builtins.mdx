---
title: builtins
description: The builtins module in gopy.
---

# `builtins`

The `builtins` module. Behaviour matches CPython 3.14. Source lives at
`module/builtins/` in the gopy repo (or under `stdlib/builtins.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
