---
title: types
description: The types module in gopy.
---

# `types`

The `types` module. Behaviour matches CPython 3.14. Source lives at
`module/types/` in the gopy repo (or under `stdlib/types.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
