---
title: contextlib
description: The contextlib module in gopy.
---

# `contextlib`

The `contextlib` module. Behaviour matches CPython 3.14. Source lives at
`module/contextlib/` in the gopy repo (or under `stdlib/contextlib.py` for
pure-Python ports).

## Status

See [manual/status](../manual/status) for current coverage.

## API

(Reference table in progress.)
